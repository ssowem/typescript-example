{
  "compilerOptions": {
    // "target" 생성되는 js코드 버전을 적용
    // ESNext => 자바스크립트 최신버전을 의미
    "target": "ESNext",
    // "module" 변환되는 js코드의 모듈 시스템을 설정
    "module": "ESNext",
    // "outDir" 컴파일 결과로 생성될 js 파일들의 위치를 설정
    "outDir": "dist",
    // "strict" 엄격한 타입 검사 (true,false)
    "strict": true,
    // 자동으로 개별모듈로 취급되도록 만들어주는 옵션
    "moduleDetection": "force",
  },
  // "include" src 디렉토리 안에있는 모든파일을 포함해서 컴파일하도록 설정
  "include": ["src"],
  "skipLibCheck": true
}
